# API server URLs (selected at runtime based on hostname)
VITE_API_LOCAL_URL=http://localhost:9000
VITE_API_PROD_URL=https://api.arandomsitein.space

# Virtual API server URLs (for preview/virtual execution)
VITE_VIRTUAL_API_LOCAL_URL=http://localhost:9001
VITE_VIRTUAL_API_PROD_URL=https://virtual.api.arandomsitein.space

# Editor base URL (for production deployment under subdirectory)
# Development: leave empty (runs at root)
# Production: set to /editor for deployment at /editor/
VITE_EDITOR_URL=
