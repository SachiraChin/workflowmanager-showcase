/**
 * Adapters for bridging @wfm/shared to webui-specific implementations.
 *
 * These adapters allow the shared rendering components to access webui's
 * state management and API without direct coupling.
 */

export { WebUIRenderProvider } from "./WebUIRenderProvider";
