# API server URLs (selected at runtime based on hostname)
VITE_API_LOCAL_URL=http://localhost:9000
VITE_API_PROD_URL=https://api.arandomsitein.space

# Editor URL (for linking to workflow editor)
# Development: use empty string or http://localhost:5174 if running separately
# Production: use /editor for subdirectory deployment
VITE_EDITOR_URL=/editor
